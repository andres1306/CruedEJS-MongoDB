<%- include('partials/header');%>

<div class="container">
  <form action="/add" method="post">
  <div class="row border border-dark rounded col-8 offset-2" style="background: #EFEFEF;">
      <div class="col-2 offset-2">
        <label for="">Nombre</label><br>
        <input type="text" placeholder="Nombre" name="Nombre">
        <label for="">Direccion</label>
        <input type="text" name="Direccion" id=""placeholder="cll00 #00-00 " name="Direccion"><br>
        <label for="">Pan</label><br>
        <select name="pan">
          <optgroup label="panes">
            <option>Panqueso </option>
            <option>Pan debono</option>
            <option>Buñuelos</option>
            <option>pan de yuca </option>
            <option>Pancerotis</option>
            <option>Pan de canela</option>
           </optgroup>
          </optgroup>
        </select><br>

      </div>

      <div class="col-2 offset-2">  
        <label for="">Documento</label> <br>
        <input type="text"name="Documento" placeholder="321654987"> <br>
        <label for="">Telefono</label> <br>
        <input type="text" name="Telefono" placeholder="987654321">
        <label for="">Cantidad</label><br>
        <input type="number" name="cantidad">

      </div>
      <div class="col-2 offset-4">
        <button class="btn btn-info" type="submit" style="margin-top: 10px;">Guardar</button>
      </div>
      <input  class="invisible"type="number" name="precio">
    </div>
  </form>
  </div>
  <div class="container">
    <table class="table" style="margin-top: 40px;">
      <thead class="thead-dark">
        <tr>
          <th class="text-center"> # </th>
          <th class="text-center"> Documento</th>
          <th class="text-center"> Nombre</th>
          <th class="text-center"> Telefono</th>
          <th class="text-center"> Pan</th>
          <th class="text-center"> Cantidad</th>
          <th class="text-center"> Precio</th>
          <th class="text-center"> Direccion</th>
          <th class="text-center" class="text-center"> Actions</th>
        </tr>
      </thead>
    <tbody>
      <% for(var i=0; i< PEDIDOS.length; i++) { %>
      <tr>
        <td class="text-center"> <%= i+1 %></td>
        <td class="text-center"><%= PEDIDOS[i].Documento %></td>
        <td class="text-center"><%= PEDIDOS[i].Nombre %></td> 
        <td class="text-center"><%= PEDIDOS[i].Telefono%></td>
        <td class="text-center"><%= PEDIDOS[i].pan%></td>
        <td class="text-center"><%= PEDIDOS[i].cantidad%></td>
        <td class="text-center"><%= PEDIDOS[i].precio%></td>
        <td class="text-center"><%= PEDIDOS[i].Direccion%></td>
        <td class="text-center"> <a onclick="confirmar(<%= PEDIDOS[i].Documento%>)" class="btn btn-danger"> Eliminar</a> 
        <a href="/edit/<%= PEDIDOS[i]._id%>" class="btn btn-success">Edit</a>
        <!--
          <button href="" onclick="confirmar(<%= PEDIDOS[i].Documento%>)" class="btn btn-success"> aler t</button>
        -->
      </td>
      </tr>
      <% } %>
      </tbody>
    </table>
  </div>
</body>
</html>
<style>
input:focus {
  background: #DFF6FF;
}
</style>}
<script src="https://cdn.jsdelivr.net/npm/sweetalert2@10.14.0/dist/sweetalert2.all.min.js"></script>
<script>
  function confirmar(id){
    Swal.fire({
  title: 'Estas seguro?',
  icon: 'warning',
  showCancelButton: true,
  confirmButtonColor: '#3085d6',
  cancelButtonColor: '#d33',
  confirmButtonText: 'Si, borrar'
}).then((result) => {
  if (result.isConfirmed) {
    window.location='/Eliminar/'+id;
  }
});
}
</script>
